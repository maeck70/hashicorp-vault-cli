# Project Guidelines

## Build & Output Constraints

- **Build Output**: All compiled Go binaries must always be placed into the `bin/` directory (e.g. `bin/<binary-name>`). Never output binaries into the project root.
